# Evaluation

Target home for duplicate-detection evaluation helpers.

Responsibilities:

- Maintain threshold sweeps.
- Compare SHA-256, MinHash, SimHash, and embedding results.
- Track false positives and false negatives with golden labels.
- Keep generated reports out of source code.

For current guide-only tooling, see:

- `guide/demo/dedup-detect-url-review/README.md`
