# Exact Layer

Target home for SHA-256 exact duplicate checks.

Responsibilities:

- Normalize text consistently.
- Compute stable fingerprints.
- Support document-level and chunk-level exact checks.
- Emit matches that are safe to mark as exact duplicates.

Current code: `src/agentic_rag/ingestion/dedup_detect/exact.py`.
