# SimHash Layer

Target home for the existing SimHash implementation.

Responsibilities:

- Build token shingles.
- Compute compact fingerprints.
- Compare fingerprints by Hamming distance.
- Emit lexical near-duplicate matches.

Current code: `src/agentic_rag/ingestion/dedup_detect/simhash.py`.
