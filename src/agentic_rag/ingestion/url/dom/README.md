# DOM Analysis

Target home for DOM-aware block detection.

Responsibilities:

- Detect repeated subtrees.
- Identify product cards, vehicle cards, FAQ items, policy sections, and tables.
- Preserve DOM paths and section context.
- Emit candidate semantic blocks before chunking.

This folder supports the strategy in `guide/dom-aware-chunking-strategy.md`.
