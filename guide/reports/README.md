# Guide Reports

This folder stores local report-style notes for ingestion evaluation, benchmark summaries, and real-data assessment writeups.

Current reports:

- `ingestion_benchmark.md`: benchmark note copied from the local project workspace.

Keep generated raw artifacts in `guide/results` or module-local data folders. Use this folder for human-readable conclusions and summaries.
